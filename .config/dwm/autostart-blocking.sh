#!/bin/bash

# This command will block, to add commands which follow this, fork this command
# like, `slstatus &`. However, consider moving this to the autostart.sh file instead
# then, as it will no longer be blocking.
slstatus
